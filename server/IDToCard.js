module.exports = function (id) {

    if(id == -1) {
        return new require('./cards/test')();
        return new card();
    }
    else if(id == -2) {
        return new require('./cards/test2')();
        return new card();
    }
}
